# 游戏网站模板使用指南（中文版）

将 Rue Valley 网站模板改造为您自己的游戏网站

---

## 🚀 快速开始


### 必需准备的信息

- ✅ 游戏名称
- ✅ 游戏口号/标语
- ✅ Steam App ID（如果在 Steam 上）
- ✅ 主题颜色（HEX 代码）
- ✅ 背景图片/横幅图片
- ✅ 宣传截图
- ✅ YouTube 视频 ID
- ✅ Reddit 社区链接

---

## 📝 核心替换清单

### 1. 项目基础信息

| 文件 | 位置 | 需要替换的内容 |
|------|------|---------------|
| `package.json` | 第 2 行 | 项目名称 |
| `src/app/layout.tsx` | 第 20-21 行 | 网站标题和描述 |
| `src/app/layout.tsx` | 第 41 行 | 背景图片路径 |

### 2. 首页内容（`src/app/page.tsx`）

| 行号 | 内容 | 说明 |
|------|------|------|
| 13 | 游戏名称 | 主标题 H1 |
| 16 | 游戏标语 | 副标题 H2 |
| 19 | 价值主张 | 网站提供什么 |
| 22-24 | 游戏描述 | 详细介绍 |
| 153-184 | 快速导航 | 6 个导航卡片 |
| 186-203 | FAQ | 4-5 个常见问题 |
| 92-119 | 核心特性 | 3 个游戏特色 |

### 3. 组件替换

| 组件 | 文件路径 | 操作 |
|------|---------|------|
| Header | `src/components/Header.tsx` 第 10 行 | 替换游戏名称 |
| Footer | `src/components/Footer.tsx` 第 11、14、47、53 行 | 替换站点信息 |
| SteamGauge | `src/components/rue-valley/SteamGauge.tsx` | Steam 统计（可选） |
| IntentionSearch | `src/components/rue-valley/IntentionSearch.tsx` | 游戏特定功能（可选） |

### 4. 数据文件

| 文件 | 内容 | 替换内容 |
|------|------|---------|
| `src/data/videos.ts` | YouTube 视频 | 替换 3-4 个视频 ID |
| `src/data/reddit.ts` | Reddit 讨论 | 替换 4-5 个帖子链接 |
| `scripts/fetch-steam.js` 第 4 行 | Steam App ID | 替换为你的游戏 ID |

### 5. 图片资源

| 路径 | 用途 | 建议尺寸 |
|------|------|---------|
| `public/images/backgrounds/hero-bg.jpg` | 首页背景 | 1920x1080+ |
| `public/images/steam/official-banner.png` | 游戏横幅 | 1600x900 |
| `public/images/screenshots/` | 游戏截图 | 自定义 |

### 6. 内容文章

替换 `src/content/` 下所有文件夹的内容：

```
src/content/
├── guide/           # 游戏攻略
├── buy/             # 购买选项
├── review/          # 评测
├── technical/       # 技术指南
├── community/       # 社区内容
├── download/        # 下载（MOD、存档等）
├── info/            # 游戏信息
└── news/            # 新闻更新
```

---

## 🎨 颜色主题替换

### 当前 Rue Valley 主题色

- 主色：`#25AB2B` (绿色)
- 深色：`#1E8923`

### 替换步骤

1. **全局搜索替换：**
```bash
# 在 src/ 目录下查找所有颜色引用
grep -r "#25AB2B" src/
grep -r "\\[#25AB2B\\]" src/
```

2. **批量替换为你的颜色：**
- 查找：`#25AB2B`
- 替换为：`#YOUR_COLOR`

3. **更新 Tailwind 配置（可选）：**
`tailwind.config.ts`:
```typescript
colors: {
  primary: '#YOUR_COLOR',
}
```

---

## 🔧 组件处理指南

### SteamGauge（Steam 数据仪表盘）

**保留条件：** 游戏在 Steam 上架

**配置步骤：**
1. 更新 `scripts/fetch-steam.js` 第 4 行的 App ID
2. 运行 `npm run fetch:steam`
3. 验证 `public/data/steamspy.json` 生成成功

**删除步骤：**
1. 删除 `src/components/rue-valley/SteamGauge.tsx`
2. 从 `src/app/page.tsx` 删除导入和使用
3. 删除 `scripts/fetch-steam.js`
4. 从 `package.json` 删除 `fetch:steam` 脚本

### IntentionSearch（意向树搜索）

**说明：** 这是 Rue Valley 游戏特有的功能组件

**选项 A - 删除（推荐）：**
1. 删除 `src/components/rue-valley/IntentionSearch.tsx`
2. 从 `src/app/page.tsx` 删除导入和使用
3. 删除 `public/data/intentions.json`

**选项 B - 改造：**
1. 重命名为你的游戏功能（如 `QuestSearch.tsx`）
2. 修改数据结构和标签
3. 创建对应的 JSON 数据文件

### VideoSection & RedditSection

**说明：** 通用组件，只需更新数据源

**操作：**
- 只需更新 `src/data/videos.ts` 和 `src/data/reddit.ts`
- 无需修改组件代码

---

## 📂 文件夹重命名

### 重命名游戏特定组件文件夹

```bash
# 从
src/components/rue-valley/

# 改为
src/components/your-game/
```

### 更新所有导入语句

在所有文件中查找并替换：
```tsx
// 从
import { Component } from '@/components/rue-valley/...'

// 改为
import { Component } from '@/components/your-game/...'
```

---

## 📋 详细替换步骤

### 第一步：克隆项目
```bash
git clone https://github.com/libin257/ruevalley.org.git
cd ruevalley.org
npm install
```

### 第二步：修改项目基础信息
1. 修改 `package.json` 中的项目名
2. 修改 `src/app/layout.tsx` 中的标题和描述
3. 修改 `src/app/layout.tsx` 中的背景图片路径

### 第三步：替换首页内容
1. 打开 `src/app/page.tsx`
2. 按照上方表格替换所有游戏相关文本
3. 更新快速导航、FAQ、核心特性

### 第四步：更新导航和页脚
1. 修改 `src/components/Header.tsx` 游戏名称
2. 修改 `src/components/Footer.tsx` 站点信息

### 第五步：替换数据文件
1. 更新 `src/data/videos.ts` YouTube 视频
2. 更新 `src/data/reddit.ts` Reddit 讨论
3. 更新 `scripts/fetch-steam.js` Steam App ID

### 第六步：替换图片资源
1. 准备背景图（1920x1080+）放入 `public/images/backgrounds/`
2. 准备游戏横幅放入 `public/images/steam/`
3. 添加其他截图和素材

### 第七步：替换颜色主题
1. 全局搜索 `#25AB2B`
2. 批量替换为你的主题色
3. 更新 Tailwind 配置（可选）

### 第八步：更新内容文章
1. 删除 `src/content/` 下所有 `.mdx` 文件
2. 创建你自己的文章，使用以下格式：

```mdx
---
title: "文章标题"
description: "简短描述"
category: "Guide"
priority: 1
date: "2025-01-17"
---

# 你的内容

用 Markdown 编写...
```

### 第九步：测试
```bash
npm run dev
# 访问 http://localhost:3000
# 测试所有链接和页面
```

### 第十步：部署
```bash
npm run build
npm run start

# 或部署到 Vercel
git push origin main
# 在 Vercel 导入项目
```

---

## ✅ 上线前检查清单

- [ ] 所有 "Rue Valley" 文本已替换
- [ ] 所有图片已替换
- [ ] Steam App ID 已更新
- [ ] YouTube 视频已更新
- [ ] Reddit 链接已更新
- [ ] 所有导航链接正常
- [ ] 所有分类页面显示正常
- [ ] 所有文章正常渲染
- [ ] 主题色已全部更新
- [ ] 元数据（标题、描述）已更新
- [ ] 页脚链接和版权已更新
- [ ] 移动端响应式测试
- [ ] 页面加载性能检查

---

## 🐛 常见问题

### Q: Steam 数据不显示？
**A:** 检查 Steam App ID 是否正确，运行 `npm run fetch:steam`

### Q: 文章不显示？
**A:** 检查 MDX 文件是否有正确的 frontmatter，文件扩展名是否为 `.mdx`

### Q: 图片不加载？
**A:** 确保图片在 `public/` 文件夹中，路径以 `/` 开头

### Q: 颜色没有更新？
**A:** 清除缓存 `rm -rf .next && npm run dev`，检查是否有遗漏的硬编码颜色

---

## 📧 技术支持

- 原始仓库：https://github.com/libin257/ruevalley.org
- 提交 Issue
- 联系：[你的联系方式]

---

**祝你的游戏网站开发顺利！🎮**
